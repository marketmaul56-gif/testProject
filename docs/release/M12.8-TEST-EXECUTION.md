# M12.8 — Integration, Security, Reliability & Performance Test Execution

Status: **🔒 LOCKED — PASS**

Execution basis: locked M12 contract + actual implementation on `m12/implementation-foundation` + accepted Change Review `CHANGE-REVIEW-M12.8-PERFORMANCE-BASELINE.md`.

Final post-approval evidence: M12 CI run `34698980079` on commit `93650cbe2b9a8a2eea6573242456a999b27eff2e`.

## Build

M12.8 uses executable release-test coverage rather than checklist-only evidence:

- `scripts/apply-migrations.sh` — deterministic, fail-fast migration runner;
- `tests/e2e/canonical-journey.test.ts` — canonical learner/instructor service E2E including AI outage and non-authority cohort completion;
- `tests/integration/failure-matrix.test.ts` — DB timeout, competency read outage, duplicate submission, cross-tenant persistence rejection;
- `tests/migration/migration-failure.sh` — injected migration failure and fail-stop verification;
- `tests/performance/read-model-baseline.test.ts` — representative MVP p95 performance gate;
- `.github/workflows/m12-ci.yml` — architecture/security/build/migration/integration/E2E/failure/performance jobs.

## Critical Review

The review preserved two boundaries:

1. performance cannot PASS without an objective threshold;
2. hosted CI verifier contract evidence is not equivalent to dedicated production-like gVisor/runsc hostile-host rehearsal, which remains an M12.9 environment gate.

The original M11 numeric performance values could not be recovered. No historical value was fabricated. A formal Change Review was performed and explicitly accepted by the user on 2026-09-12.

## Revision

The accepted Change Review established the following bounded replacement baseline only for the server-side read-model gate:

| Read path | Approved p95 target |
|---|---:|
| Learner overview | <= 100 ms |
| Instructor cohort overview | <= 200 ms |

Dataset minimum:

- 50 learners;
- 20 lessons;
- 1,000 lesson-progress rows.

The performance test now asserts these thresholds and fails the CI job when either target is exceeded.

## Functional / Quality Gate

### Final CI result

M12 CI run `34698980079`: **9/9 jobs PASS**.

| Layer / scenario | Executed evidence | Result |
|---|---|---|
| Architecture / static typing | `domain-architecture-security` | PASS |
| Unit / domain invariants | `pnpm run test:domain` | PASS |
| Authorization / input security | `pnpm run test:security` | PASS |
| AI + verifier contracts / hidden-test boundary | `pnpm run test:contract` | PASS |
| Production web build | `web-build` | PASS |
| Fresh PostgreSQL migration | `migration` | PASS |
| Learner/instructor integration | `experience-integration` | PASS |
| Verification/evidence/competency authority chain | `authority-integration` | PASS |
| Cohort lifecycle / membership / assignment | `cohort-operations` | PASS |
| Canonical learner→verification→evidence→competency→instructor E2E | `release-e2e-failure` | PASS |
| AI unavailable but core flow remains usable | canonical E2E + AI contract | PASS |
| Verifier unavailable → ERROR, never learner FAIL | authority integration | PASS |
| DB/dependency timeout | failure matrix | PASS |
| Competency query unavailable | failure matrix | PASS |
| Duplicate submission request | failure matrix | PASS |
| Duplicate verification request | authority integration + canonical E2E | PASS |
| Duplicate enrollment | cohort operations | PASS |
| Stale lifecycle mutation | cohort operations | PASS |
| Concurrent/optimistic lifecycle protection | cohort operations | PASS |
| Unauthorized object/resource access | security regression | PASS |
| Cross-tenant access/reference | security regression + failure matrix | PASS |
| Hidden-test access/non-disclosure boundary | verifier sandbox contract | PASS (contract level) |
| Evidence persistence failure | authority integration | PASS |
| Migration failure fail-stop | `migration-failure` | PASS |
| Approved performance thresholds | `performance-baseline` | PASS |

### Canonical E2E evidence

The release E2E executes:

`cohort assignment → enrollment → structured lesson progress → AI unavailable → practice submission → authoritative verification → retry-idempotency → evidence → competency → project artifact/revision/submission → authoritative verification → evidence → competency → learner view → instructor cohort/learner view → formative guidance`.

It transitions the cohort to `COMPLETED` and verifies evidence/competency counts do not change.

Result: **PASS**.

### Failure-injection evidence

The executed matrix proves:

- AI outage does not stop the core deterministic path;
- PostgreSQL statement timeout does not create learner authority state;
- competency read outage does not fabricate competence;
- duplicate submission cannot duplicate canonical effect;
- duplicate verification/evidence is prevented by idempotency/provenance rules;
- verifier outage remains system `ERROR`, never learner `FAILED`;
- evidence persistence failure remains retryable and cannot fabricate competency;
- cross-tenant persistence references are rejected;
- stale/concurrent cohort lifecycle mutations are protected.

Result: **PASS**.

### Migration evidence

Fresh PostgreSQL 18 migration: **PASS**.

Injected invalid migration demonstrates fail-stop behavior: the invalid migration fails and later migration is not executed.

Result: **PASS**.

### Performance evidence after Change Review approval

Post-approval run `34698980079` measured:

| Read path | p50 | p95 | max | Approved p95 | Result |
|---|---:|---:|---:|---:|---|
| Learner overview | 4.15 ms | 5.84 ms | 6.33 ms | <= 100 ms | PASS |
| Instructor cohort overview | 28.24 ms | 40.39 ms | 42.01 ms | <= 200 ms | PASS |

The threshold comparison is executable in `tests/performance/read-model-baseline.test.ts`; these values are not checklist-only observations.

## Change Review resolution

`RB-M12-008-001 — Exact locked M11 numeric performance targets unavailable` is **RESOLVED** through the explicitly accepted Change Review.

Important governance note: the approved 100/200 ms values are a prospective replacement baseline. They are **not** represented as the original historical M11 values.

## Environment evidence deferred to M12.9

Dedicated production-like verifier-host execution using actual gVisor/runsc hostile workloads is not represented by GitHub-hosted CI. Contract-level verifier security evidence is PASS, while environment rehearsal remains mandatory in M12.9 before RC/production readiness.

Backup/restore, rollback/recovery rehearsal, production configuration, observability/alert validation, and production-like smoke also remain M12.9 gates.

## Final Review

- Functional integration: PASS
- Canonical authority regression: PASS
- Security/authorization regression: PASS within M12.8 scope
- Reliability/failure injection: PASS
- Migration success/fail-stop behavior: PASS
- Performance executable target comparison: PASS
- Open M12.8 blocker: NONE

**M12.8 final decision: PASS — 🔒 LOCKED.**

M12.9 Release Candidate & Deployment Readiness may now begin.
