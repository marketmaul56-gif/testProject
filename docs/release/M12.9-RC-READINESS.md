# M12.9 — Deployment Preparation & Release Candidate

Status: **🔒 LOCKED — PASS WITH EXPLICIT ENVIRONMENT BINDINGS**

Release candidate source SHA evaluated: `37de8373b5fe83383d02e0016a2e007bff8de79e`.

M12.9 closes implementation/deployability readiness. It does **not** claim that a cloud account, provider region, production OTLP backend, or live traffic deployment already exists. Those are Production Release environment bindings governed by `docs/release/DEPLOYMENT-TARGET.md`.

## Build → Critical Review → Revision

The release audit found and resolved the following implementation gaps before lock:

- production learner submission/verification runtime wiring was incomplete;
- PostgreSQL outbox was initially consumed directly instead of through the locked BullMQ/Redis delivery layer;
- project artifact proof lacked executable S3-compatible presign/upload/seal/download integration;
- OpenTelemetry runtime instrumentation was incomplete;
- verifier hostile-workload rehearsal needed real `runsc` execution rather than contract-only tests;
- S3 SigV4 checksum/metadata signing required revision before MinIO accepted the integrity contract.

The bounded runtime amendment was authorized by accepted Change Review `CHANGE-REVIEW-M12.9-RUNTIME-WIRING.md`. No competence-authority, evidence, PASS/FAIL, AI, tenant, or immutable-artifact invariant was changed.

## Final executable evidence on the same RC SHA

All seven workflows for `37de837...` completed successfully:

| Workflow | Run | Result |
|---|---:|---|
| Bootstrap Integrity | `34727454197` | PASS |
| M12 CI | `34727454184` | PASS |
| M12 Redis Authority Transport | `34727454192` | PASS |
| M12 Object Storage Integration | `34727454193` | PASS |
| M12 Recovery Drill | `34727454199` | PASS |
| M12 gVisor Hostile Rehearsal | `34727454223` | PASS |
| M12 OCI Artifact Smoke | `34727454219` | PASS |

### M12 CI

PASS evidence includes:

- frozen dependency install;
- architecture/static checks;
- domain invariant tests;
- negative authorization/input-security tests;
- AI/verifier contracts;
- Prisma validation;
- fresh PostgreSQL 18 migration and critical constraint checks;
- learner/instructor experience integration;
- canonical authority integration;
- cohort operations;
- canonical E2E and dependency-failure matrix;
- migration fail-stop;
- approved performance thresholds;
- production runtime command → worker → dispatcher → authority E2E.

### Async authority transport

Production delivery is now:

`PostgreSQL transactional outbox → BullMQ/Redis wake-up/delivery → idempotent trusted worker → canonical authority service`.

PostgreSQL remains correctness/source-of-truth authority. Redis job loss cannot erase the unpublished PostgreSQL event and does not become evidence/competency authority.

### Project artifact storage

S3-compatible integration PASS proves:

`authorized upload intent → presigned PUT → signed checksum/metadata → HEAD validation → immutable seal → trusted download`.

PostgreSQL stores artifact identity/provenance/hash/seal metadata; object storage stores bytes. Tampered metadata/checksum cannot be silently sealed as an authoritative revision.

### gVisor/runsc rehearsal

The hostile rehearsal uses actual `runsc` and verifies the locked containment contract, including runtime identity, non-root execution, no credential inheritance, read-only root filesystem, network/metadata denial, bounded CPU/memory/PID/time behavior, and cleanup. A selected production verifier host must rerun this rehearsal before first live traffic and after material host changes.

### OCI release artifacts

Provider-neutral OCI smoke PASS builds and boots:

- Next.js web;
- NestJS API;
- trusted BullMQ worker;
- verifier-dispatcher.

It also passes API liveness/readiness, worker/dispatcher process smoke, and web standalone runtime smoke.

### Migration and recovery

- deterministic version-controlled migration: PASS;
- injected migration failure fail-stop: PASS;
- logical PostgreSQL backup + checksum + restore to distinct target: PASS;
- authority/immutability/auth/cohort/audit constraints survive restore: PASS.

## Deployment target resolution

The old `RB-M12-009-002` is **not** resolved by inventing a cloud vendor. M3 deliberately deferred vendor selection. `DEPLOYMENT-TARGET.md` now defines the provider-neutral production conformance contract for PostgreSQL 18, Redis/BullMQ, S3-compatible storage, immutable OCI services, gVisor verifier capacity, secrets, OTLP/alerts, OpenTofu binding, backup/PITR, rollback, and release evidence.

Therefore provider/account/region selection is classified as a **Production Release environment binding**, not an unresolved application implementation blocker. It must be completed before first live traffic.

## Blocker resolution

- `RB-M12-009-001` production canonical runtime wiring — **RESOLVED**.
- `RB-M12-009-002` provider ambiguity — **RESOLVED AS PROVIDER-NEUTRAL CONTRACT / ENVIRONMENT BINDING**.
- `RB-M12-009-003` gVisor/runsc rehearsal — **RESOLVED for release-candidate rehearsal**; rerun on selected live verifier host is a launch prerequisite.
- `RB-M12-009-004` backup/restore — **RESOLVED**.

## Final Review

No M12.9 implementation release blocker remains. The codebase is deployable as a provider-neutral MVP release candidate. Live production deployment still requires environment-specific OpenTofu/secret/registry/PITR/alert bindings and production smoke, as documented in the launch checklist.

**Decision: M12.9 🔒 LOCKED.**
