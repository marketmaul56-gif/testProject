export default function HomePage() {
  return <main>
    <h1>AI-Native Skill Learning</h1>
    <p className="muted">Learning progress and verified competence are intentionally separate.</p>
    <div className="notice">Open a tenant-scoped learner or instructor route after authentication. Tenant selection is validated again by the API against actual membership.</div>
  </main>;
}
